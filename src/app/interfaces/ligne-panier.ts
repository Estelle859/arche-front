import { Article } from "./article";

export interface LignePanier {
    id?: number;   
    qty?: number;  
    article?: Article; 
    prix?:number;
    // libelle: String; 
    //tva:number;
    // prixttc:number;
    // prixtva:number;
    // prixht:number;
    // montant:number;

}
